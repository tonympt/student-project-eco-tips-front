// exemple to reducer
// import action type
// import {TOGGLE_IS_EXPANDED} from "@/actions/card";

//state
export const initialState = {
  state: [],
};

// reducer
const reducer = (state = initialState, action = {}) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default reducer;
